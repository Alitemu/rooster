# npm 10 crashes resolving vitest 4's optional peer set ("Cannot read properties of
# null (reading 'edgesOut')"); the lockfile was resolved with this on, and
# `npm ci` (Dockerfile) must use the same setting to accept it.
legacy-peer-deps=true
